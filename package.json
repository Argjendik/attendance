{
  "name": "attendance",
  "version": "1.0.0",
  "description": "Attendance System",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "heroku-postbuild": "cd frontend && npm install && chmod +x node_modules/.bin/vite && npm run build && cd ../backend && npm install && npm run build && mkdir -p dist && cp -r ../frontend/dist/* dist/",
    "start": "cd backend && npm start"
  }
} 