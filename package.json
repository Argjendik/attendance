{
  "name": "attendance-system",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "18.x",
    "npm": "10.x"
  },
  "scripts": {
    "heroku-postbuild": "cd backend && npm install --production=false && chmod +x ./node_modules/.bin/prisma && chmod +x ./node_modules/.bin/nest && npx prisma generate && npm run build && cp -r dist/ ../dist/ && npm prune --production",
    "start": "cd backend && NODE_ENV=production node dist/main"
  },
  "cacheDirectories": ["backend/node_modules"]
} 