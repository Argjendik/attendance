{
  "name": "attendance",
  "version": "1.0.0",
  "description": "Attendance System",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "heroku-postbuild": "cd frontend && npm install && npm run build && mkdir -p ../backend/dist && cp -r dist/* ../backend/dist/ && cd ../backend && npm install && npm run build",
    "start": "cd backend && npm start"
  }
} 