{
  "name": "attendance-system",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "18.x",
    "npm": "10.x"
  },
  "scripts": {
    "build": "cd backend && npm install && node ./node_modules/prisma/build/index.js generate && node ./node_modules/@nestjs/cli/bin/nest.js build",
    "start": "cd backend && NODE_ENV=production node dist/main.js"
  },
  "cacheDirectories": ["backend/node_modules"]
} 