{
  "name": "attendance-system",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "18.x",
    "npm": "10.x"
  },
  "scripts": {
    "heroku-postbuild": "cd backend && npm install --production=false && chmod +x ./node_modules/.bin/prisma && chmod +x ./node_modules/.bin/nest && npx prisma generate && npx nest build && ls -la dist && npm prune --production",
    "start": "cd backend && NODE_ENV=production node dist/main.js"
  },
  "cacheDirectories": ["backend/node_modules"]
} 