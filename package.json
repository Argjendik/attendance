{
  "name": "attendance-system",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "18.x",
    "npm": "10.x"
  },
  "scripts": {
    "build": "cd backend && npm install && npx prisma generate && npx nest build",
    "start": "cd backend && NODE_ENV=production node dist/main.js",
    "heroku-postbuild": "cd backend && npm install && npx prisma generate && npx nest build"
  },
  "cacheDirectories": ["backend/node_modules"]
} 