{
  "name": "attendance-system",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "18.x",
    "npm": "10.x"
  },
  "scripts": {
    "heroku-postbuild": "cd backend && npm install --production=false && cd .. && npm install -g @nestjs/cli && cd backend && nest build && pwd && ls -la && ls -la dist && npm prune --production",
    "start": "cd backend && NODE_ENV=production node dist/main.js"
  },
  "cacheDirectories": ["backend/node_modules"]
} 