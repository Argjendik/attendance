{
  "name": "attendance-system",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "18.x",
    "npm": "10.x"
  },
  "scripts": {
    "heroku-postbuild": "cd backend && npm install && npx prisma generate && npm run build",
    "start": "cd backend && npm run start:prod"
  },
  "cacheDirectories": ["backend/node_modules"]
} 