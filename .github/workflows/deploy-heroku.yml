name: Deploy to Heroku

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18.x'

      - name: Install Heroku CLI
        run: npm install -g heroku

      - name: Setup Heroku Postgres
        env:
          HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
          HEROKU_APP_NAME: ${{ secrets.HEROKU_APP_NAME }}
        run: |
          heroku addons:create heroku-postgresql:mini -a ${{ secrets.HEROKU_APP_NAME }}
          DB_URL=$(heroku config:get DATABASE_URL -a ${{ secrets.HEROKU_APP_NAME }})
          echo "Database URL: $DB_URL"

      - name: Setup Heroku Config
        env:
          HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
          HEROKU_APP_NAME: ${{ secrets.HEROKU_APP_NAME }}
        run: |
          heroku config:set \
            DATABASE_URL=$(heroku config:get DATABASE_URL -a ${{ secrets.HEROKU_APP_NAME }}) \
            JWT_SECRET=${{ secrets.JWT_SECRET }} \
            NODE_ENV=production \
            PORT=80 \
            HOST=0.0.0.0 \
            CORS_ORIGIN=${{ secrets.FRONTEND_URL }} \
            -a ${{ secrets.HEROKU_APP_NAME }}

      - name: Deploy Backend to Heroku
        env:
          HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
          HEROKU_APP_NAME: ${{ secrets.HEROKU_APP_NAME }}
        working-directory: ./backend
        run: |
          heroku container:login
          git init
          git add .
          git commit -m "Deploy to Heroku"
          heroku git:remote -a ${{ secrets.HEROKU_APP_NAME }}
          git push heroku HEAD:main --force 