{
  "name": "attendance",
  "version": "1.0.0",
  "description": "Attendance System",
  "scripts": {
    "heroku-postbuild": "cd frontend && npm install && npm run build && cd ../backend && npm install && npm run build && cp -r ../frontend/dist .",
    "start": "cd backend && npm start"
  }
} 