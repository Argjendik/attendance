{
  "name": "attendance",
  "version": "1.0.0",
  "description": "Attendance System",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "heroku-postbuild": "npm install -g vite && cd frontend && npm install && npm run build && cd ../backend && npm install && npm run build && mkdir -p dist && cp -r ../frontend/dist/* dist/",
    "start": "cd backend && npm start"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "typescript": "^5.0.0"
  }
} 