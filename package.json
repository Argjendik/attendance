{
  "name": "attendance-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "build": "cd frontend && npm install && npm run build",
    "heroku-postbuild": "cd frontend && npm install && npm install -g typescript && npm run build"
  },
  "engines": {
    "node": "18.x",
    "npm": "10.x"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
} 